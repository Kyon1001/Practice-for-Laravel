[phases.setup]
nixPkgs = ["php81", "php81Packages.composer"]

[phases.install]
cmds = [
    "cd src && composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "cd src && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT" 